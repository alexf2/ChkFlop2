#if !defined(_LIMITS_OPT_)
#define _LIMITS_OPT_

const unsigned int
  LIM_Queue_MIN   = 1,
  LIM_Queue_MAX   = 25,

  LIM_Retry_MIN   = 0,
  LIM_Retry_MAX   = 20,

  LIM_Index_MIN   = 1,
  LIM_Index_MAX   = 0xFFFE,

  LIM_DOSMem_MIN  = 5,
  LIM_DOSMem_MAX  = 0xFFFF;


#endif

